// JavaScript Document
$(document).ready(function(e)

$('#izquierda´). swipe left (function (){
	navigator.notification.confirm ('¿QUE DESEA HACER?; function (opcion){
		switch (opcion){
			case 1:
			navigator.notification.beep (1);
			break;
			case 2:
			navigator.notification.vibrate (500);
			break;
		}//switch
	}"practica 1" "beep, vibrar, cancelar"); //function confirm
});//function swipe
{
});